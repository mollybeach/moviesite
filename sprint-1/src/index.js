import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';  //add scss because we aren't compile
import App from './components/app/app';

ReactDOM.render(<App />, document.getElementById('root'));


